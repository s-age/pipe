feat: Add role_options to session dashboard BFF endpoint

This commit introduces the following changes:

- Modified `openapi.yml` to update the `RoleOption` schema's `value` description to prepend 'roles/'.
- Modified `src/pipe/core/repositories/role_repository.py` to prepend 'roles/' to the `value` of `RoleOption` when retrieving role options.
- Modified `src/pipe/web/controllers/session_detail_controller.py` to include `role_options` in the response of the `/api/v1/bff/session-dashboard/{session_id}` endpoint.
- Modified `src/pipe/web/actions/get_roles_action.py` to fix a `mypy` type error by ensuring the error return type matches the declared return type.
- Modified `tests/web/test_app.py` to add a new test case for the `/api/v1/bff/session-dashboard/{session_id}` endpoint, including assertions for `role_options`, and fixed `ruff` line length issues.
- Ensured all `ruff format`, `ruff check`, and `mypy` checks pass across the project.