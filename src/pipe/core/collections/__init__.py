"""
The collections package, providing collection classes for domain entities.

Note: To avoid circular imports, classes are not re-exported from __init__.py.
Import them directly from their respective modules:
  from pipe.core.collections.backup_files import BackupFiles
"""
