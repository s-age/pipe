"""
The domains package, providing domain logic and business rules.

Note: This package contains domain logic functions and classes.
To avoid circular imports, classes are not re-exported from __init__.py.
Import them directly from their respective modules:
  from pipe.core.domains.gemini_api_stream_processor import GeminiApiStreamProcessor
"""
