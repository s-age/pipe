import { style } from '@vanilla-extract/css'

export const aside = style({
  display: 'block'
})

export const positionLeft = style({
  // This is a placeholder for potential left-specific styling
})

export const positionRight = style({
  // This is a placeholder for potential right-specific styling
})
