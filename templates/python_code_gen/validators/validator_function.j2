{% for dep in dependencies %}
{{ dep }}
{% endfor %}


def validate_{{ name.lower() }}(value: str) -> None:
    """
    {{ description }}
    """
    # TODO: Implement validation logic here
    if not value:
        raise ValueError("Value cannot be empty.")
